
% -----------------------------------------------------------------------------
function feat = max_abs(diff)
% -----------------------------------------------------------------------------
feat = max(abs(diff), 1);

